<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.golden72">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_hit_people"
        android:label="@string/app_name"
        android:roundIcon="@color/black"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".backpack.BackpackActivity"></activity>
        <activity android:name=".building.BuildingActivity" />
        <activity android:name=".desert.DesertActivity" />
        <activity android:name=".activity01.M01Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>